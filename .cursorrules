# VK Bot Expert System - Правила постоянного контекста

## ПРОЕКТ: VK Бот с ИИ системой и рангами
**Версия:** 6.0 (Fusionbot)
**Язык:** Python 3.11+
**Основные технологии:** VK API 5.199+, Groq AI, SQLite, AsyncIO

## АРХИТЕКТУРА ПРОЕКТА
```
Fusionbot/
├── vk_bot.py          # Основной класс бота
├── ai_system.py       # ИИ система (Groq + локальные ответы)
├── database.py        # SQLite база данных с рангами
├── console_admin.py   # Консольная панель администратора
├── ТОКЕНЫ.env         # Переменные окружения
├── requirements.txt   # Зависимости
└── vk_bot.db         # База данных SQLite
```

## ПРАВИЛА РАЗРАБОТКИ

### 1. ПОСТОЯННАЯ ПАМЯТЬ
- ВСЕГДА читай project_overview.md при начале работы
- Обновляй progress.md после каждого изменения
- Записывай важные решения в decisions.md
- Используй Memory Bank для контекста между сессиями

### 2. VK API ИНТЕГРАЦИЯ
- Используй VK API версии 5.199+ (актуальная)
- Всегда проверяй валидность токенов
- Работай только с беседами (peer_id > 2000000000)
- Используй Long Poll для получения сообщений
- Применяй rate limiting (3 сек между сообщениями)

### 3. СОВРЕМЕННЫЙ PYTHON
- Используй async/await для всех I/O операций
- Применяй type hints для всех функций
- Следуй принципам SOLID
- Добавляй подробные русские комментарии
- Используй logging вместо print

### 4. ОБРАБОТКА ОШИБОК
- Всегда оборачивай VK API вызовы в try/except
- Логируй все ошибки с контекстом
- Имей fallback для демо-режима
- Валидируй все входящие данные

### 5. ИИ СИСТЕМА
- Groq API как основной (если доступен)
- Локальные ответы как fallback
- Контекстные ответы для разных ситуаций
- Генерация шуток, историй, комплиментов

### 6. БАЗА ДАННЫХ
- SQLite с автобекапом
- Система рангов (1-10 уровней)
- Статистика пользователей
- Админские права по чатам

### 7. БЕЗОПАСНОСТЬ
- Никогда не логируй токены
- Валидируй все входящие данные
- Защита от флуда
- Проверка прав администратора

## КОМАНДЫ БОТА
- `помощь` / `help` - справка
- `тест` / `test` - тестовое сообщение
- `ии [вопрос]` - общение с ИИ
- Приветствия обрабатываются автоматически

## ТОКЕНЫ И ПЕРЕМЕННЫЕ
- VK_TOKEN - токен VK группы
- VK_GROUP_ID - ID группы VK
- GROQ_API_KEY - ключ для Groq AI

## СТАТУС ПРОЕКТА
- ✅ Базовая архитектура готова
- ✅ Демо-режим работает
- ⚠️ Требуется валидация VK токена
- 🔄 В разработке: расширенные команды

## СЛЕДУЮЩИЕ ШАГИ
1. Исправить интеграцию токенов
2. Добавить расширенные команды
3. Улучшить ИИ систему
4. Добавить тесты
5. Настроить мониторинг

## ПРИНЦИПЫ КАЧЕСТВЕННОГО КОДА
- Читаемость превыше всего
- Документируй все публичные методы
- Используй константы для магических чисел
- Разделяй ответственность между модулями
- Тестируй критичные функции

## АВТОМАТИЗАЦИЯ
- При открытии проекта анализируй текущее состояние
- Предлагай следующие шаги разработки
- Отслеживай прогресс в progress.md
- Создавай TODO списки для задач

## CONTEXT7 ИНТЕГРАЦИЯ
- ВСЕГДА используй Context7 для получения актуальной документации
- При каждом запросе о VK API автоматически используй Context7
- При генерации кода проверяй актуальность через Context7
- Для VK API используй версию 5.199+ (проверяй через Context7)
- Для Python библиотек используй версии 2025 года
- Автоматически подставляй свежие примеры кода из Context7
- При сомнениях в актуальности API - используй Context7 для проверки
